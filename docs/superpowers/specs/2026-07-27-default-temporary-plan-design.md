# 新增计划默认选择临时计划

## 目标

普通打开“新增计划”弹窗时，“选择项目”默认选中“临时计划（不使用项目）”，避免误把临时事项归入列表中的第一个项目。

## 行为

- “今日计划 → 新增计划”和“本周目标 → 安排行动”等所有现有新增入口，项目选择初始值均为空，对应“临时计划（不使用项目）”。
- 用户仍可在下拉框中选择任意有效项目。
- 编辑已有计划的逻辑不变。

## 实现与验证

只调整快速新增表单的初始 `projectId`，不修改领域数据结构和保存命令。增加针对默认选择的回归测试，并运行相关测试、类型检查和构建。
